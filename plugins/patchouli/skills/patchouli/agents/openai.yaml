interface:
  display_name: "Patchouli"
  short_description: "Capture, evolve, and query Obsidian knowledge"
  default_prompt: "Use $patchouli to capture or update what I learned as a reviewed knowledge card."

dependencies:
  tools:
    - type: "mcp"
      value: "patchouli"
      description: "Local Patchouli launch, checkpoint, vault, review, update, and search tools"

policy:
  allow_implicit_invocation: true
